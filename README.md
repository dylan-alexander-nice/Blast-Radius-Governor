# Blast-Radius-Governor
A governance layer that scores every autonomous CX agent action for blast radius before it executes — safe actions run instantly, risky ones get staged or routed to a human, automatically.
